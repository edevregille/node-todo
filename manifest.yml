applications:
- name:      todoappmanu123

# you may need to use a different hostname, if someone has already used this one

  host:      todoappmanu123
  command:   node server --verbose -d mongo
  memory:    128M
  services:
  - MongoDB by Compose-2w
